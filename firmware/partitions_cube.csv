# cube-light custom partition table (4MB flash).
#
# Grows the two OTA app slots from the Arduino default 1.25MB to 1.875MB
# each (+50% headroom for the pattern catalog). NVS stays at 0x9000/0x5000
# — IDENTICAL to the default table — so a serial upload (which does not
# full-erase) preserves every saved setting (WiFi creds, per-pattern
# params, up-axis, LED/mic config).
#
# Changing this table requires ONE flash over USB (esptool rewrites the
# bootloader + table + app). OTA can only replace an app, not redraw the
# map. After this, OTA resumes with the larger slots.
#
# Name,     Type, SubType, Offset,   Size,     Flags
nvs,        data, nvs,     0x9000,   0x5000,
otadata,    data, ota,     0xe000,   0x2000,
app0,       app,  ota_0,   0x10000,  0x1E0000,
app1,       app,  ota_1,   0x1F0000, 0x1E0000,
spiffs,     data, spiffs,  0x3D0000, 0x20000,
coredump,   data, coredump,0x3F0000, 0x10000,
